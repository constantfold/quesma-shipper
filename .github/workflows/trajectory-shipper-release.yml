# Releases for the trajectory shipper: every push to main that touches the shipper publishes one
# through TUF in R2, then announces it on GitHub.
#
# VERSION is the reviewed release line. The script appends <commit-count>.<hash>, keeping builds
# within that line ordered and carrying provenance that buildinfo verifies before trusting.

name: trajectory-shipper-release

on:
  push:
    branches: [main]
    # Narrower than CI's filter on purpose: a docs-only commit should not make
    # every enrolled machine download a byte-identical-in-behavior new shipper.
    paths:
      - "src/**"
      - "scripts/release-version.sh"
      - "VERSION"
      - "Makefile"
      - ".github/workflows/trajectory-shipper-release.yml"
  schedule:
    # Refresh short-lived TUF metadata even when no shipper release was published.
    - cron: "17 3 * * *"
  workflow_dispatch:
    inputs:
      recover_expired_repository:
        description: "Allow one refresh to recover already-expired metadata"
        required: true
        default: false
        type: boolean

permissions:
  contents: read

jobs:
  version:
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      value: ${{ steps.version.outputs.value }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Compute the release version
        id: version
        run: echo "value=$(sh scripts/release-version.sh)" >> "$GITHUB_OUTPUT"

      - name: Reserve the release tag
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_VERSION: ${{ steps.version.outputs.value }}
        run: |
          if tag_sha=$(gh api "repos/$GITHUB_REPOSITORY/git/ref/tags/$RELEASE_VERSION" --jq .object.sha 2>/dev/null); then
            [[ "$tag_sha" == "$GITHUB_SHA" ]] || {
              echo "::error::Release tag $RELEASE_VERSION points at $tag_sha, expected $GITHUB_SHA"
              exit 1
            }
            echo "Release tag $RELEASE_VERSION already points at $GITHUB_SHA"
            exit 0
          fi

          gh api --method POST "repos/$GITHUB_REPOSITORY/git/refs" \
            -f ref="refs/tags/$RELEASE_VERSION" -f sha="$GITHUB_SHA" >/dev/null

  build:
    if: github.event_name == 'push'
    needs: version
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
        with:
          # The version IS the history: rev-list --count needs all of it.
          fetch-depth: 0

      - uses: actions/setup-go@v7
        with:
          go-version-file: src/go.mod
          cache-dependency-path: src/go.sum

      - name: Build the platform matrix
        run: make dist RELEASE_VERSION=${{ needs.version.outputs.value }}

      - name: Create the release manifest
        run: |
          jq -n \
            --arg version "${{ needs.version.outputs.value }}" \
            --arg published_at "$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
            '{
              version: $version,
              published_at: $published_at,
              targets: {
                "linux/amd64": "quesma-shipper-linux-amd64",
                "linux/arm64": "quesma-shipper-linux-arm64",
                "darwin/amd64": "quesma-shipper-darwin-amd64",
                "darwin/arm64": "quesma-shipper-darwin-arm64",
                "windows/amd64": "quesma-shipper-windows-amd64.exe",
                "windows/arm64": "quesma-shipper-windows-arm64.exe",
                "windows/amd64/setup": "QuesmaShipperSetup-amd64.exe",
                "windows/arm64/setup": "QuesmaShipperSetup-arm64.exe",
                "darwin/pkg": "quesma-shipper-macos-universal.pkg"
              }
            }' > bin/dist/release.json

      - uses: actions/upload-artifact@v7
        with:
          # Windows signs its two payloads before promoting this intermediate build for release.
          name: shipper-build-binaries
          path: |
            bin/dist/quesma-shipper-linux-*
            bin/dist/quesma-shipper-windows-*
            bin/dist/release.json
          compression-level: 0
          retention-days: 7

  build-windows-setup:
    if: github.event_name == 'push'
    needs: [version, build]
    runs-on: windows-2025
    timeout-minutes: 15
    permissions:
      contents: read
    # Windows signing is disabled pending organization validation; restore its steps from git history.
    steps:
      - uses: actions/checkout@v7
      - uses: actions/download-artifact@v8
        with:
          name: shipper-build-binaries
          path: bin/dist
      - name: Build the per-user setup executables
        shell: pwsh
        run: |
          foreach ($arch in @("amd64", "arm64")) {
            src/packaging/windows/build-setup.ps1 `
              -ReleaseVersion "${{ needs.version.outputs.value }}" `
              -Architecture $arch `
              -BinaryPath "bin/dist/quesma-shipper-windows-$arch.exe" `
              -SupervisorPath "bin/dist/quesma-shipper-windows-$arch-supervisor.exe" `
              -OutputDir "bin/dist"
          }
      - uses: actions/upload-artifact@v7
        with:
          name: shipper-release-binaries
          path: |
            bin/dist/quesma-shipper-linux-*
            bin/dist/quesma-shipper-windows-*
            bin/dist/QuesmaShipperSetup-*.exe
            bin/dist/release.json
          compression-level: 0
          retention-days: 7

  build-macos:
    if: github.event_name == 'push'
    needs: version
    runs-on: macos-15
    timeout-minutes: 30
    environment:
      name: tuf-publishing-testing
      deployment: false
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v7
        with:
          go-version-file: src/go.mod
          cache-dependency-path: src/go.sum

      - uses: apple-actions/import-codesign-certs@v7
        id: application-certificate
        with:
          p12-file-base64: ${{ secrets.MACOS_APPLICATION_CERT_P12_B64 }}
          p12-password: ${{ secrets.MACOS_APPLICATION_CERT_P12_PASSWORD }}

      - uses: apple-actions/import-codesign-certs@v7
        with:
          keychain: signing_temp
          create-keychain: false
          keychain-password: ${{ steps.application-certificate.outputs.keychain-password }}
          p12-file-base64: ${{ secrets.MACOS_INSTALLER_CERT_P12_B64 }}
          p12-password: ${{ secrets.MACOS_INSTALLER_CERT_P12_PASSWORD }}

      - name: Build and notarize the macOS release
        env:
          APPLE_NOTARY_ID: ${{ vars.APPLE_NOTARY_ID }}
          APPLE_NOTARY_TEAM_ID: ${{ vars.APPLE_NOTARY_TEAM_ID }}
          APPLE_NOTARY_PASSWORD: ${{ secrets.APPLE_NOTARY_PASSWORD }}
          MACOS_APPLICATION_IDENTITY: "Developer ID Application: Quesma Poland Sp. z o.o. (66ZNG89HN6)"
          MACOS_INSTALLER_IDENTITY: "Developer ID Installer: Quesma Poland Sp. z o.o. (66ZNG89HN6)"
        run: |
          make macos-pkg RELEASE_VERSION="${{ needs.version.outputs.value }}"
          src/packaging/macos/pkg/notarize.sh bin/dist

      - uses: actions/upload-artifact@v7
        with:
          name: shipper-release-macos
          path: |
            bin/dist/quesma-shipper-macos-universal.pkg
            bin/dist/quesma-shipper-darwin-amd64
            bin/dist/quesma-shipper-darwin-arm64
          compression-level: 0
          retention-days: 7

  check-tuf-root:
    name: Check offline TUF root expiry
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7
      - name: Enforce the 30-day renewal window
        run: |
          root=$(find src/packaging/common/roots -name '*.root.json' -print 2>/dev/null | sort -V | tail -1)
          [[ -f "$root" ]] || { echo '::error::Commit the offline-signed roots/1.root.json before merging'; exit 1; }
          expires=$(jq -er '.signed.expires | strings' "$root")
          expiry=$(date -d "$expires" +%s)
          remaining=$(( expiry - $(date +%s) ))
          (( remaining >= 2592000 )) || { echo '::error::TUF root expires within 30 days; perform the offline renewal ceremony'; exit 1; }

  prepare-tuftool:
    name: Prepare tuftool
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/cache/restore@v5
        id: tuftool-cache
        with:
          path: ~/.cache/quesma-ci/tuftool/0.17.0
          key: tuftool-v1-${{ runner.os }}-${{ runner.arch }}-0.17.0

      - name: Build tuftool
        if: steps.tuftool-cache.outputs.cache-hit != 'true'
        run: |
          tool_dir="$HOME/.cache/quesma-ci/tuftool/0.17.0"
          install_dir="$RUNNER_TEMP/tuftool-install"
          mkdir -p "$tool_dir"
          cargo install tuftool --version 0.17.0 --locked --root "$install_dir"
          install -m 0755 "$install_dir/bin/tuftool" "$tool_dir/tuftool"
          cd "$tool_dir"
          sha256sum tuftool > tuftool.sha256

      - name: Verify tuftool
        run: |
          tool_dir="$HOME/.cache/quesma-ci/tuftool/0.17.0"
          cd "$tool_dir"
          sha256sum --check --strict tuftool.sha256
          [[ "$(./tuftool --version)" == "tuftool 0.17.0" ]]

      - uses: actions/cache/save@v5
        if: steps.tuftool-cache.outputs.cache-hit != 'true'
        with:
          path: ~/.cache/quesma-ci/tuftool/0.17.0
          key: tuftool-v1-${{ runner.os }}-${{ runner.arch }}-0.17.0

      - uses: actions/upload-artifact@v7
        with:
          name: release-tuftool
          path: |
            ~/.cache/quesma-ci/tuftool/0.17.0/tuftool
            ~/.cache/quesma-ci/tuftool/0.17.0/tuftool.sha256
          compression-level: 0
          retention-days: 1

  publish-tuf:
    name: ${{ github.event_name == 'push' && 'Publish release to TUF/R2' || github.event_name == 'workflow_dispatch' && 'Recover TUF metadata' || 'Refresh TUF metadata' }}
    needs: [build, build-macos, build-windows-setup, check-tuf-root, prepare-tuftool]
    # A scheduled run skips build; every non-skipped prerequisite must still succeed.
    if: ${{ !failure() && !cancelled() }}
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    # Serialize only R2 publication, so a release awaiting approval cannot block refreshes.
    concurrency:
      group: tuf-publication
      cancel-in-progress: false
    permissions:
      contents: read
    environment:
      name: tuf-publishing-testing
      deployment: false
    steps:
      - uses: actions/checkout@v7

      - uses: actions/download-artifact@v8
        with:
          name: release-tuftool
          path: ${{ runner.temp }}/tuftool

      - name: Verify tuftool
        run: |
          cd "$RUNNER_TEMP/tuftool"
          sha256sum --check --strict tuftool.sha256
          chmod 0755 tuftool
          [[ "$(./tuftool --version)" == "tuftool 0.17.0" ]]
          echo "$RUNNER_TEMP/tuftool" >> "$GITHUB_PATH"

      - uses: actions/download-artifact@v8
        if: github.event_name == 'push'
        with:
          pattern: shipper-release-*
          path: artifacts
          merge-multiple: true

      - name: Download current TUF metadata
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          AWS_ENDPOINT_URL: ${{ vars.R2_ENDPOINT }}
          AWS_REGION: auto
          R2_URI: ${{ vars.R2_URI }}
        run: |
          mkdir -p .tuf/metadata artifacts
          aws s3 sync "${R2_URI%/}/metadata/" .tuf/metadata/ --no-progress

      - name: Sign TUF metadata
        env:
          TUF_SIGNING_KEY_B64: ${{ secrets.TUF_SIGNING_KEY_B64 }}
          ADD_TARGETS: ${{ github.event_name == 'push' }}
          RECOVER_EXPIRED_REPOSITORY: ${{ github.event_name == 'workflow_dispatch' && inputs.recover_expired_repository }}
        run: |
          umask 077
          key="$RUNNER_TEMP/tuf-operational.pem"
          echo "::add-mask::$key"
          trap 'rm -f "$key"' EXIT
          install -m 0600 /dev/null "$key"
          printf '%s' "$TUF_SIGNING_KEY_B64" | base64 --decode > "$key"
          [[ -s "$key" ]] || { echo '::error::Decoded TUF signing key is empty'; exit 1; }

          version=$(date +%s)
          week=$(date -u -d '+7 days' +%Y-%m-%dT%H:%M:%SZ)
          common=(--key "$key"
            --targets-expires "$(date -u -d '+90 days' +%Y-%m-%dT%H:%M:%SZ)" --targets-version "$version"
            --snapshot-expires "$week" --snapshot-version "$version"
            --timestamp-expires "$week" --timestamp-version "$version" --outdir .tuf)
          [[ "$ADD_TARGETS" != true ]] || common+=(--add-targets artifacts)
          root=$(find .tuf/metadata -name '*.root.json' -print | sort -V | tail -1)

          if [[ -z "$root" ]]; then
            tuftool create --root src/packaging/common/roots/1.root.json "${common[@]}"
          else
            recovery=()
            [[ "$RECOVER_EXPIRED_REPOSITORY" != true ]] || recovery=(--allow-expired-repo)
            tuftool update --root "$root" "${common[@]}" "${recovery[@]}" --metadata-url "file://$PWD/.tuf/metadata/"
          fi

      - name: Publish to R2
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          AWS_ENDPOINT_URL: ${{ vars.R2_ENDPOINT }}
          AWS_REGION: auto
          R2_URI: ${{ vars.R2_URI }}
        run: |
          if [[ -d .tuf/targets ]]; then
            while IFS= read -r -d '' target; do
              object=${target#.tuf/targets/}
              filename=${object#*.}
              [[ "$object" =~ ^[0-9a-f]{64}\.[A-Za-z0-9._-]+$ ]] || {
                echo "::error::Unexpected TUF target name: $object"
                exit 1
              }
              aws s3 cp "$target" "${R2_URI%/}/targets/$object" \
                --content-disposition "attachment; filename=\"$filename\"" --no-progress
            done < <(find -L .tuf/targets -type f -print0)
          fi
          aws s3 sync .tuf/metadata/ "${R2_URI%/}/metadata/" --exclude timestamp.json --no-progress
          aws s3 cp .tuf/metadata/timestamp.json "${R2_URI%/}/metadata/timestamp.json" --no-progress

  announce-github-release:
    name: Announce release on GitHub
    if: github.event_name == 'push'
    needs: [version, publish-tuf]
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7

      - uses: actions/download-artifact@v8
        with:
          pattern: shipper-release-*
          path: artifacts
          merge-multiple: true

      - name: Generate the Homebrew cask from the published binaries
        env:
          RELEASE_VERSION: ${{ needs.version.outputs.value }}
        run: python3 scripts/homebrew-cask.py "$RELEASE_VERSION" artifacts > quesma-shipper.rb

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_VERSION: ${{ needs.version.outputs.value }}
        run: |
          if gh release view "$RELEASE_VERSION" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            gh release upload "$RELEASE_VERSION" quesma-shipper.rb --clobber --repo "$GITHUB_REPOSITORY"
            exit 0
          fi

          gh release create "$RELEASE_VERSION" quesma-shipper.rb \
            --repo "$GITHUB_REPOSITORY" \
            --title "Quesma Shipper $RELEASE_VERSION" \
            --notes-file - <<EOF
          Quesma Shipper **$RELEASE_VERSION** is now available.

          Every installer below installs for the current user without administrator rights. Released builds keep themselves current from the signed update channel.

          | Platform | Installer |
          |---|---|
          | macOS 13+ | [Download quesma-shipper-macos-universal.pkg](https://updates.quesma.dev/download/quesma-shipper-macos-universal.pkg) (.pkg installer, Apple Silicon and Intel), or with Homebrew \`brew install --cask quesmaorg/tap/quesma-shipper\` |
          | Windows 10 1809+ | [Download QuesmaShipperSetup-amd64.exe](https://updates.quesma.dev/download/QuesmaShipperSetup-amd64.exe) (.exe installer, x64) or [QuesmaShipperSetup-arm64.exe](https://updates.quesma.dev/download/QuesmaShipperSetup-arm64.exe) (.exe installer, Arm64) |
          | Linux | Run the installer script below (x64 or Arm64) |

              curl -fsSLO https://raw.githubusercontent.com/QuesmaOrg/quesma-shipper/main/src/packaging/linux/install.sh
              sh install.sh

          The Linux script downloads a hash-pinned bootstrap binary and installs a systemd user service. Windows release signing is temporarily disabled, so Microsoft Defender SmartScreen may warn about the download.

          The attached \`quesma-shipper.rb\` is the cask for this exact release. The [Homebrew tap](https://github.com/QuesmaOrg/homebrew-tap) imports it automatically, so \`brew\` users never need to download it by hand.

          Portable binaries without service integration: Linux [x64](https://updates.quesma.dev/download/quesma-shipper-linux-amd64) · Linux [Arm64](https://updates.quesma.dev/download/quesma-shipper-linux-arm64) · Windows [x64](https://updates.quesma.dev/download/quesma-shipper-windows-amd64.exe) · Windows [Arm64](https://updates.quesma.dev/download/quesma-shipper-windows-arm64.exe)
          EOF
